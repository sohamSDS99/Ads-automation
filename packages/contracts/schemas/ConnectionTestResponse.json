{
  "$defs": {
    "CredentialKind": {
      "enum": [
        "openrouter",
        "google_ads",
        "dataforseo",
        "webshare",
        "smtp"
      ],
      "title": "CredentialKind",
      "type": "string"
    }
  },
  "description": "The outcome of the cheapest real call that proves a source answers.",
  "properties": {
    "kind": {
      "$ref": "#/$defs/CredentialKind"
    },
    "ok": {
      "title": "Ok",
      "type": "boolean"
    },
    "detail": {
      "description": "The sentence to show the person who pressed Test",
      "title": "Detail",
      "type": "string"
    },
    "meta": {
      "additionalProperties": true,
      "description": "Non-secret hints, e.g. account name or credit remaining",
      "title": "Meta",
      "type": "object"
    },
    "tested_at": {
      "format": "date-time",
      "title": "Tested At",
      "type": "string"
    },
    "recorded": {
      "description": "Whether the verdict was stored. A disconnected source has no row to write it to, so its test answers the question and keeps no state.",
      "title": "Recorded",
      "type": "boolean"
    }
  },
  "required": [
    "kind",
    "ok",
    "detail",
    "tested_at",
    "recorded"
  ],
  "title": "ConnectionTestResponse",
  "type": "object",
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
