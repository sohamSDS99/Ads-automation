{
  "description": "The minimum a project needs to exist. Everything else is the wizard's job.",
  "properties": {
    "name": {
      "maxLength": 120,
      "minLength": 1,
      "title": "Name",
      "type": "string"
    },
    "domain": {
      "maxLength": 253,
      "minLength": 3,
      "title": "Domain",
      "type": "string"
    }
  },
  "required": [
    "name",
    "domain"
  ],
  "title": "CreateProjectRequest",
  "type": "object",
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
