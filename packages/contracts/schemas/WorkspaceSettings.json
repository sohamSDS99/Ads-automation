{
  "description": "The workspace-wide defaults `/settings` edits.\n\nBoth are *defaults*: `llm.router` lets a project override the models, and\n`orchestrator.executor` lets a project override the cap. What is set here\napplies to every project that has not said otherwise.",
  "properties": {
    "models": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "Task class \u2192 OpenRouter model id",
      "title": "Models",
      "type": "object"
    },
    "max_run_cost_usd": {
      "anyOf": [
        {
          "exclusiveMinimum": 0.0,
          "type": "number"
        },
        {
          "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Hard ceiling per research run. Null falls back to the deployment default.",
      "title": "Max Run Cost Usd"
    },
    "max_plan_cost_usd": {
      "anyOf": [
        {
          "exclusiveMinimum": 0.0,
          "type": "number"
        },
        {
          "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Hard ceiling per campaign-plan run (Stage 02 \u00a717 PF4). Its own key on purpose: a plan is a different shape of job from a research run, and a workspace raising the research ceiling must not silently raise this one. Null falls back to the deployment default.",
      "title": "Max Plan Cost Usd"
    }
  },
  "title": "WorkspaceSettings",
  "type": "object",
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
