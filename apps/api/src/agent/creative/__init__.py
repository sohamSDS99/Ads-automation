"""Stage 04's pure and near-pure creative modules (PRD §6.1).

`constants.py` reads `creative_constants.yaml`; `lint_adapter.py` is the only
Stage 04 caller of `guardrails.linter.lint()`; `brief.py` renders, measures and
hashes the brief 4.1.1 writes and G7 approves.
"""
